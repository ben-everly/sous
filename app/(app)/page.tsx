export default function Home() {
  // TODO(launch): placeholder — replace with a real first-run state (e.g. "add your
  // first recipe") once inventory/recipe features exist; a bare banner is a dead end.
  return (
    <div className="p-6">
      <h1 className="text-3xl font-bold tracking-tight">Dashboard</h1>
      <p className="text-muted-foreground">Welcome to Sous, your home kitchen management app.</p>
    </div>
  )
}
